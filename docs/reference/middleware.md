# Middleware reference

Middleware wraps capability execution and may transform the request or the
result before passing control to the next handler.

## Objects

- `Invocation`
- `Middleware`
- `MiddlewareChain`
