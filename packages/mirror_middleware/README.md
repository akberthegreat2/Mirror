# Mirror Middleware

Mirror Middleware contains the built-in middleware descriptors and
implementations used by Mirror.

## Common middleware

- retry
- timeout
- rate limit
- logging
- tracing

Middleware is where Mirror handles cross-cutting work without changing the
pipeline itself.
