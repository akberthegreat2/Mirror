# Mirror Compliance

Capability package for deterministic policy and compliance checks.

Use this package with `mirror-core` to validate knowledge documents against
simple policy rules before downstream indexing or publication.
