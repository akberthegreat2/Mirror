# Mirror Enrich

Capability package for deterministic metadata enrichment.

Use this package with `mirror-core` to derive summary, keyword, and statistics
signals from text documents before deduplication or chunking.
